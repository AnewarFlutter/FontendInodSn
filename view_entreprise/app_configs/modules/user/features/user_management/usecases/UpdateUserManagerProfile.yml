# --------------------------------------------------------------
# updateUserManagerProfile - PATCH
# --------------------------------------------------------------
$name: updateUserManagerProfile
$description: "Modifie le profil manager d'un utilisateur (ADMIN uniquement). Permission: user.update"
$skip: true
$async: true

$params:
  - $name: user_id
    $type: string
    $optional: false
  - $name: payload
    $type: UpdateUserManagerProfilePayload
    $related: "--module user --feature user_management --kind custom --step feature"
    $optional: false

$returnDescription: "Confirmation de la mise à jour du profil manager"
$returnType:
  $type: string
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: USER.UPDATE_USER_MANAGER_PROFILE
  $mapping:
    $path:
      user_id: user_id
    $body:
      notes: payload.notes
      responsabilites: payload.responsabilites
  $responseMapping:
    $returnValue: message

$imports:
  - $name: UpdateUserManagerProfilePayload
    $from: "--module user --feature user_management --kind custom --step feature"
    $step: feature
