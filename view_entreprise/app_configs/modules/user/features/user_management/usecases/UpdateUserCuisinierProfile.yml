# --------------------------------------------------------------
# updateUserCuisinierProfile - PATCH
# --------------------------------------------------------------
$name: updateUserCuisinierProfile
$description: "Modifie le profil cuisinier d'un utilisateur. Permission: user.update"
$skip: true
$async: true

$params:
  - $name: user_id
    $type: string
    $optional: false
  - $name: payload
    $type: UpdateUserCuisinierProfilePayload
    $related: "--module user --feature user_management --kind custom --step feature"
    $optional: false

$returnDescription: "Confirmation de la mise à jour du profil cuisinier"
$returnType:
  $type: string
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: USER.UPDATE_USER_CUISINIER_PROFILE
  $mapping:
    $path:
      user_id: user_id
    $body:
      specialites: payload.specialites
      poste_principal: payload.poste_principal
      allergenes_maitrises: payload.allergenes_maitrises
  $responseMapping:
    $returnValue: message

$imports:
  - $name: UpdateUserCuisinierProfilePayload
    $from: "--module user --feature user_management --kind custom --step feature"
    $step: feature
