# --------------------------------------------------------------
# updateUserLivreurProfile - PATCH
# --------------------------------------------------------------
$name: updateUserLivreurProfile
$description: "Modifie le profil livreur d'un utilisateur. Permission: user.update"
$skip: true
$async: true

$params:
  - $name: user_id
    $type: string
    $optional: false

$returnDescription: "Confirmation de la mise à jour du profil livreur"
$returnType:
  $type: string
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: USER.UPDATE_USER_LIVREUR_PROFILE
  $mapping:
    $path:
      user_id: user_id
  $responseMapping:
    $returnValue: message
