# --------------------------------------------------------------
# getMyPermissions - GET
# --------------------------------------------------------------
$name: getMyPermissions
$description: "Récupère les permissions effectives de l'administrateur connecté (rôles + permissions individuelles)"
$skip: true
$async: true

$params: []

$returnDescription: "Liste des permissions effectives"
$returnType:
  $type: string
  $list: true
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: USER.GET_MY_PERMISSIONS
  $responseMapping:
    $returnValue: permissions
