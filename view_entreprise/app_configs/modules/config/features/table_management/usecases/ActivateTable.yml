# --------------------------------------------------------------
# ActivateTable - POST
# --------------------------------------------------------------
$name: activateTable
$description: "Active une table par son identifiant"
$skip: false
$async: true

$params:
  - $name: tableId
    $type: string
    $optional: false

$returnDescription: "La table activée"
$returnType:
  $type: TableBasic
  $nullable: true
  $related:
    - $name: TableBasic
      $module: config
      $kind: custom
      $step: module

$rewriteInController: true
$rewriteInDataSourceImpl: false
$rewriteInDataSource: true
$rewriteInRepository: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: CONFIG.ACTIVATE_TABLE
  $mapping:
    $path:
      table_id: tableId
  $responseMapping:
    $returnValue: ""

$imports:
  - $name: TableBasic
    $from: "--module config --kind custom --step module"
    $step: module
