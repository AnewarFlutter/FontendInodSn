# --------------------------------------------------------------
# DeleteItemType - DELETE
# --------------------------------------------------------------
$name: deleteItemType
$description: "Supprime un type d'item par son identifiant"
$skip: false
$async: true

$params:
  - $name: itemTypeId
    $type: string
    $optional: false

$returnDescription: "Confirmation de suppression"
$returnType:
  $type: object
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$rewriteInDataSource: true
$rewriteInRepository: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: CONFIG.DELETE_ITEM_TYPE
  $mapping:
    $path:
      item_type_id: itemTypeId
  $responseMapping:
    $returnValue: ""
