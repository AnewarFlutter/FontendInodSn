# --------------------------------------------------------------
# DeleteDynamicConfigByKey - DELETE
# --------------------------------------------------------------
$name: deleteDynamicConfigByKey
$description: "Supprime une configuration dynamique par sa clé"
$skip: false
$async: true

$params:
  - $name: configKey
    $type: string
    $optional: false

$returnDescription: "Confirmation de suppression"
$returnType:
  $type: object
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$rewriteInDataSource: true
$rewriteInRepository: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: CONFIG.DELETE_DYNAMIC_CONFIG_BY_KEY
  $mapping:
    $path:
      config_key: configKey
  $responseMapping:
    $returnValue: ""
