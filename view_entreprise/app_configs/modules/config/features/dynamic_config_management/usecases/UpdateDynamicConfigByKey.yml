# --------------------------------------------------------------
# UpdateDynamicConfigByKey - PUT
# --------------------------------------------------------------
$name: updateDynamicConfigByKey
$description: "Met à jour une configuration dynamique par sa clé"
$skip: false
$async: true

$params:
  - $name: configKey
    $type: string
    $optional: false
  - $name: payload
    $type: UpdateDynamicConfigByKeyPayload
    $optional: false
    $related: "--module config --feature dynamic_config_management --kind custom --step feature"

$returnDescription: "La configuration dynamique mise à jour"
$returnType:
  $type: DynamicConfigBasic
  $nullable: true
  $related:
    - $name: DynamicConfigBasic
      $module: config
      $kind: custom
      $step: module

$rewriteInController: true
$rewriteInDataSourceImpl: true
$rewriteInDataSource: true
$rewriteInRepository: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: CONFIG.UPDATE_DYNAMIC_CONFIG_BY_KEY
  $mapping:
    $path:
      config_key: configKey
    $body:
      payload: payload
  $responseMapping:
    $returnValue: ""

$imports:
  - $name: UpdateDynamicConfigByKeyPayload
    $from: "--module config --feature dynamic_config_management --kind custom --step feature"
    $step: feature
  - $name: DynamicConfigBasic
    $from: "--module config --kind custom --step module"
    $step: module
