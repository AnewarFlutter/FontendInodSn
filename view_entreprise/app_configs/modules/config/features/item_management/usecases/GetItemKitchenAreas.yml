# --------------------------------------------------------------
# GetItemKitchenAreas - GET
# --------------------------------------------------------------
$name: getItemKitchenAreas
$description: "Récupère les zones cuisine associées à un item"
$skip: false
$async: true

$params:
  - $name: itemId
    $type: string
    $optional: false

$returnDescription: "Les zones cuisine de l'item"
$returnType:
  $type: object
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$rewriteInDataSource: true
$rewriteInRepository: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: CONFIG.GET_ITEM_KITCHEN_AREAS
  $mapping:
    $path:
      item_id: itemId
  $responseMapping:
    $returnValue: ""
