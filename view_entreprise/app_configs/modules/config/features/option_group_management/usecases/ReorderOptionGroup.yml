# --------------------------------------------------------------
# ReorderOptionGroup - PATCH
# --------------------------------------------------------------
$name: reorderOptionGroup
$description: "Réordonne un groupe d'options"
$skip: false
$async: true

$params:
  - $name: optionGroupId
    $type: string
    $optional: false
  - $name: new_order
    $type: number
    $optional: false

$returnDescription: "Résultat du réordonnancement"
$returnType:
  $type: object
  $nullable: true

$rewriteInController: true
$rewriteInDataSourceImpl: true
$rewriteInDataSource: true
$rewriteInRepository: true
$implementationInDataSource:
  $type: api
  $client: fetch
  $endpoint: CONFIG.REORDER_OPTION_GROUP
  $mapping:
    $path:
      option_group_id: optionGroupId
    $body:
      new_order: new_order
  $responseMapping:
    $returnValue: ""
