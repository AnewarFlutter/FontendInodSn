# ------------------------------------------------------------------
# DELETE_DYNAMIC_CONFIG - Supprimer une configuration dynamique
# ------------------------------------------------------------------
$path: /config/config/{config_id}/
$method: DELETE
$description: "Supprimer une configuration dynamique par ID"
$auth: true
$skip: false
$params:
  $path:
    config_id: { $type: number, $optional: false }
$response:
  $default:
    $description: "204 No Content"
    $schema:
      message: { $type: string }
  $byStatus:
    "404":
      $schema:
        detail: { $type: string }
