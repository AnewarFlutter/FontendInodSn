# ------------------------------------------------------------------
# DELETE_OPTION_GROUP - Supprimer un groupe d'options
# ------------------------------------------------------------------
$path: /config/option-groups/{option_group_id}/
$method: DELETE
$description: "Supprimer un groupe d'options"
$auth: true
$skip: false
$params:
  $path:
    option_group_id: { $type: number, $optional: false }
$response:
  $default:
    $description: "204 No Content"
    $schema:
      message: { $type: string }
  $byStatus:
    "404":
      $schema:
        detail: { $type: string }
