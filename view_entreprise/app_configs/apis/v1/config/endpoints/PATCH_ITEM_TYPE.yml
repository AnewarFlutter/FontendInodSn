# ------------------------------------------------------------------
# PATCH_ITEM_TYPE - Mettre à jour partiellement un type d'item
# ------------------------------------------------------------------
$path: /config/item-types/{item_type_id}/
$method: PATCH
$description: "Mettre à jour partiellement un type d'item"
$auth: true
$skip: false
$params:
  $path:
    item_type_id: { $type: number, $optional: false }
  $body:
    name: { $type: string, $optional: true }
    is_active: { $type: boolean, $optional: true }
$response:
  $default:
    $schema:
      id: { $type: number }
      name: { $type: string }
      is_active: { $type: boolean }
  $byStatus:
    "400":
      $schema:
        error: { $type: string }
    "404":
      $schema:
        detail: { $type: string }
