# ------------------------------------------------------------------
# ACTIVATE_TABLE - Activer une table
# ------------------------------------------------------------------
$path: /config/tables/{table_id}/activate/
$method: POST
$description: "Activer une table"
$auth: true
$skip: false
$params:
  $path:
    table_id: { $type: number, $optional: false }
  $body: {}
$response:
  $default:
    $schema:
      message: { $type: string }
  $byStatus:
    "404":
      $schema:
        detail: { $type: string }
