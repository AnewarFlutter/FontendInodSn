# ------------------------------------------------------------------
# DEACTIVATE_KITCHEN - Désactiver une cuisine
# ------------------------------------------------------------------
$path: /config/kitchens/{kitchen_id}/deactivate/
$method: POST
$description: "Désactiver une cuisine par son identifiant"
$auth: true
$skip: false
$params:
  $path:
    kitchen_id: { $type: string, $optional: false }
$response:
  $default:
    $schema:
      id: { $type: string }
      name: { $type: string }
      slug: { $type: string }
      description: { $type: string, $nullable: true }
      icon: { $type: string, $nullable: true }
      area: { $type: string, $nullable: true }
      area_name: { $type: string, $nullable: true }
      responsable_id: { $type: string, $nullable: true }
      is_active: { $type: boolean }
  $byStatus:
    "404":
      $schema:
        detail: { $type: string }
