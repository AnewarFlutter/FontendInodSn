# ------------------------------------------------------------------
# REORDER_OPTION_GROUP - Réordonner un groupe d'options
# ------------------------------------------------------------------
$path: /config/option-groups/{option_group_id}/reorder/
$method: PATCH
$description: "Modifier l'ordre d'affichage d'un groupe d'options"
$auth: true
$skip: false
$params:
  $path:
    option_group_id: { $type: number, $optional: false }
  $body:
    new_order: { $type: number, $optional: false }
$response:
  $default:
    $schema:
      message: { $type: string }
  $byStatus:
    "400":
      $schema:
        error: { $type: string }
    "404":
      $schema:
        detail: { $type: string }
