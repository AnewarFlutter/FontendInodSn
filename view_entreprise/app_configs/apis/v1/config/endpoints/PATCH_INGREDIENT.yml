# ------------------------------------------------------------------
# PATCH_INGREDIENT - Modifier partiellement un ingrédient
# ------------------------------------------------------------------
$path: /config/ingredients/{ingredient_id}/
$method: PATCH
$description: "Modifier partiellement un ingrédient"
$auth: true
$skip: false
$params:
  $path:
    ingredient_id: { $type: number, $optional: false }
  $body:
    name: { $type: string, $optional: true }
    is_active: { $type: boolean, $optional: true }
$response:
  $default:
    $schema:
      id: { $type: number }
      name: { $type: string }
      is_active: { $type: boolean }
  $byStatus:
    "400":
      $schema:
        error: { $type: string }
