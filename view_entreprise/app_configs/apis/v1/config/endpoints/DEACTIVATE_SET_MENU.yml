# ------------------------------------------------------------------
# DEACTIVATE_SET_MENU - Désactiver une formule
# ------------------------------------------------------------------
$path: /config/set-menus/{set_menu_id}/deactivate/
$method: POST
$description: "Désactiver une formule"
$auth: true
$skip: false
$params:
  $path:
    set_menu_id: { $type: number, $optional: false }
$response:
  $default:
    $schema:
      message: { $type: string }
      id: { $type: number }
      is_active: { $type: boolean }
  $byStatus:
    "404":
      $schema:
        detail: { $type: string }
