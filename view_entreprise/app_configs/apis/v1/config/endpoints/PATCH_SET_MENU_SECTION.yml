# ------------------------------------------------------------------
# PATCH_SET_MENU_SECTION - Modifier partiellement une section de formule
# ------------------------------------------------------------------
$path: /config/set-menu-sections/{set_menu_section_id}/
$method: PATCH
$description: "Modifier partiellement une section de formule"
$auth: true
$skip: false
$params:
  $path:
    set_menu_section_id: { $type: number, $optional: false }
  $body:
    max_selections: { $type: number, $optional: true }
    category_id: { $type: number, $optional: true }
$response:
  $default:
    $schema:
      id: { $type: number }
      set_menu: { $type: number }
      category_id: { $type: number }
      max_selections: { $type: number }
  $byStatus:
    "400":
      $schema:
        error: { $type: string }
