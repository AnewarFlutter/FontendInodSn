# ------------------------------------------------------------------
# DELETE_AREA - Supprimer une zone
# ------------------------------------------------------------------
$path: /config/areas/{area_id}/
$method: DELETE
$description: "Supprimer une zone — retourne 204 No Content"
$auth: true
$skip: false
$params:
  $path:
    area_id: { $type: number, $optional: false }
$response:
  $default:
    $description: "204 No Content"
    $schema:
      message: { $type: string }
  $byStatus:
    "404":
      $schema:
        detail: { $type: string }
